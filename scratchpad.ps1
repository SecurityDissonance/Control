scratchpad


#filters out old date requires a date field to exist, if filtering is done before can be commented out
#$testdata = $testdata | where {$_.date -gt $olddate}
#$data | Add-Member  -MemberType NoteProperty -Name $inputos -Value "test"
